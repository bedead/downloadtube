import { styles } from "@/app/style";

export default function Footer() {
    return (
        <footer>
            <div className={`${styles.padding}  text-center`}>
                Footer
            </div>
        </footer>
    )
}