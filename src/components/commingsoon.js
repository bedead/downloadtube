import { styles } from "@/app/style";

export default function CommingSoon() {
    return (
        <div className={`${styles.padding} h-[50%] items-center text-center`}>
            <p className="font-black text-4xl animate-pulse">Comming Soon...</p>
        </div>
    )
}